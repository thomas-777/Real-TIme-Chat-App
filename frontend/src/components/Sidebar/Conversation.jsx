
import React from "react";

export default function Conversation({key,name,onClick}) {
    return (
        <div>
            <button>
                <p>name</p>  
            </button>
        </div>
    );
}